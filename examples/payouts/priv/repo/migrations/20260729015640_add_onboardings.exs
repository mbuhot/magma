defmodule Payouts.Repo.Migrations.AddOnboardings do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:onboardings, primary_key: false) do
      add(:id, :uuid, null: false, default: fragment("uuid_generate_v7()"), primary_key: true)
      add(:destination_currency, :text, null: false)
      add(:status, :text, null: false, default: "pending")
      add(:account_ref, :text)
      add(:identity_ref, :text)

      add(:inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )

      add(:updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
      )

      add(
        :customer_id,
        references(:customers,
          column: :id,
          name: "onboardings_customer_id_fkey",
          type: :uuid,
          prefix: "public"
        ), null: false)
    end

    create(
      unique_index(:onboardings, [:customer_id, :destination_currency],
        name: "onboardings_one_per_rail_index"
      )
    )
  end

  def down do
    drop(constraint(:onboardings, "onboardings_customer_id_fkey"))

    drop_if_exists(
      unique_index(:onboardings, [:customer_id, :destination_currency],
        name: "onboardings_one_per_rail_index"
      )
    )

    drop(table(:onboardings))
  end
end
